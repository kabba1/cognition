# Deliberate attention engineering gate

**1,703 tests pass** in one clean full PostgreSQL 18 run on Python 3.13/Windows
(644.85 seconds). Ruff check and format check pass for 182 Python files; strict
mypy passes for 83 source files. [Full test output](phase4a-tests.txt).

This increment adds 38 cases: 15 PostgreSQL selection cases, 16 pure compiler
cases and seven end-to-end acceptance cases. All earlier tests also pass.

Fresh context now includes bounded owned wake/focus reference recall and one-hop
parent selection. Direct references can recall old or terminal records outside
recent pools without reopening or strengthening them. Eight earliest active or
disputed commitments due within 24 hours receive mandatory detail. A ninth-row
notice exposes a larger urgent pool while allowing the executive to resolve its
backlog. A mandatory byte-budget overflow still fails before inference.

Optional packing gives recent families round-robin opportunities, preserving
existing active/established priorities. Snapshots record attention policy, actual
selection reasons, bounded source truncation and budget omissions. Summary space
is reserved before packing; every final request remains within its estimate.
Only rendered content contributes selected references. Already frozen requests
retain their exact context, even after state, wakes or configuration change.

Independent review found and resolved an equal-rank duplicate ordering edge case;
canonical projection ties now produce the same bytes regardless of input order.
Additional regressions preserve full BIGINT event ordering and existing recent
family priorities. Final independent review found no remaining concrete issue in
the compiler, store selection, renderer refactors or runtime integration.

Acceptance verifies fresh-model recall of an older named goal, including a completed
one; progress through nine urgent obligations across turns; correct overflow notice
when a ninth obligation is independently referenced; foreign pointers without
foreign content; mandatory-budget failure without a provider attempt; retrieval
without personal mutation; and unchanged frozen-request recovery.

No new migration, public executive schema, model provider, external action or
background service is introduced. Lexical retrieval and then heartbeat/reflection/
exploration scheduling remain subsequent Phase 4 increments. These deterministic
tests do not establish empirical long-term recall or autonomous behavior quality.
