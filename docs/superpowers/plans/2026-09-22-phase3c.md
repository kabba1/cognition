# Phase 3 relationship substrate plan

Continue user-authorized autonomous execution. Apply writing-plans, TDD,
dispatching-parallel-agents for disjoint schema/store work, requesting-code-review
and verification-before-completion. Do not begin source edits until Phase 3b's
final verification run finishes.

1. Schema task: db/models/relationships.py, migration0007_relationships,
   tests/integration/db/test_relationship_schema.py. Exact fields in design.
2. Store task: stores/relationships.py and tests/integration/test_relationship_stores.py.
   Coordinate minimal shared helper extraction with root; one individual lock,
   revision writer and caller transaction. No v1 protocol changes.
3. Root: register models/schema head/reference kinds/lock coverage; integrate
   bounded relationship/thread/project/entity context and integrity checks. Add
   runtime reference/context and diagnostic regressions.
4. Independent review, fix concrete findings, full PostgreSQL/static gate,
   documentation and publish verified checkpoint. Then design the explicit
   executive protocol extension so these objects can be created by cognition.
