# Phase 3 first increment implementation plan

Use executing-plans, test-driven-development and verification-before-completion.
Use dispatching-parallel-agents for disjoint schema and personal-store work; root
integrates policy/context/runtime. User has authorized autonomous architectural
gap filling and continuous work, superseding routine design approval stops.

Spec: ../specs/2026-09-22-phase3a-design.md

1. Schema: models/personal.py, migration0005_personal_state,
   integration/db/test_personal_schema.py. Write real PostgreSQL constraints and
   migration tests first; implement exact shared contract, verify metadata drift.
   Root registers exports and advances supported schema.
2. Personal stores: stores/personal.py, integration/test_personal_stores.py.
   Test transitions, scope, provenance/history, evidence gates, collisions, rollback.
   Implement explicit stores per spec, including entity/project create/revise APIs
   and bounded personal context sections. No writes to other assigned source files.
3. Root integration: policy/cognition.py, stores/cognition.py, runtime/context.py,
   runtime/cognition.py. Write whole-decision atomicity and restart context tests;
   enable four operation families, retain unsupported-family rejection, cap and
   validate all operation identities. Pass personal sections through snapshot.
4. Acceptance and diagnostics: check personal ownership, revisions and referenced
   evidence; add crash-after-personal-write and commit-before-restart recovery.
   Update old tests asserting personal tables absent. Extend operations/handoff docs.
5. Full PostgreSQL suite, Ruff, strict mypy, independent review. Fix findings,
   commit reviewable changes, push branch, continue Phase 3 remaining families.

## Progress

- Design written from academic master architecture and existing v1 operation schema.
- Phase 2 final gate passed: 898 tests; Ruff/format/strict mypy. Base e2b3590.
- Independent design review clarified serialized mutation locking, incompatible
  operation fields, and both-target supersession conflict detection before code.
- Completed: 1,146 tests pass on real PostgreSQL, including 147 personal-store
  transition cases, 65 schema cases, 14 personal recovery cases and 14 personal
  integrity cases. Ruff/format and strict mypy (73 source files) pass.
- Independent implementation review added exact persisted-decision guards,
  per-object context packing and active-obligation priority. An outdated birth
  table-absence assertion was replaced with an all-nonbirth-tables-empty assertion.
  Final clean full run: docs/review/phase3a-tests.txt.
